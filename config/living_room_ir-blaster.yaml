---

substitutions:
  device_name: living-room-ir-blaster
  name: "Living room IR Blaster"
  description: Tuya WiFi IR Remote

packages:
  # the device included config/common/* so it can do needed '!remove's
  device: !include devices/tuya_wifi_ir_remote.yaml
